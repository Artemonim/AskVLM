# Development dependencies for Artemonim's Speech Kit
# Install with: pip install -r requirements-dev.txt

# ! Linting and formatting
ruff>=0.1.0                 # * Fast Python linter and formatter (replaces flake8, isort, black)

# ! Static type checking
mypy>=1.7.0                 # * Static type checker for Python
types-tqdm>=4.66.0          # * Type stubs for tqdm
types-requests>=2.31.0      # * Type stubs for requests

# ! Security analysis
bandit[toml]>=1.7.5         # * Security linter for Python code

# ! Additional code quality
pylint>=3.0.0               # * Comprehensive Python linter

# ! Testing framework
pytest>=7.4.0               # * Testing framework
pytest-cov>=4.1.0           # * Coverage plugin for pytest
pytest-xdist>=3.3.0         # * Distributed testing plugin
pytest-mock>=3.11.0         # * Mock object library integration

# ! Documentation (optional)
sphinx>=7.1.0               # * Documentation generator
sphinx-rtd-theme>=1.3.0     # * Read the Docs theme for Sphinx

# ! Development utilities
ipython>=8.15.0             # * Enhanced interactive Python shell
jupyter>=1.0.0              # * Jupyter notebook support

# ! Application settings
pydantic>=1.10.0            # * Settings model support

# ! GUI
PySide6>=6.5.0              # * Qt6 bindings for GUI

# ! Media processing
ffmpeg-python>=0.2.0        # * FFmpeg wrapper for audio/video

# ! Speech-to-text (basic dependencies only for now)
# whisper>=1.0.0              # * OpenAI Whisper transcription - will be added in Phase 2
# pyannote.audio>=2.3.0       # * Speaker diarization pipeline - will be added in Phase 2
# llama-cpp-python>=0.1.104   # * Local LLM inference (Mistral GGUF) - will be added in Phase 2

# ! Torch for GPU operations with CUDA support (Windows)
# * Security: require a fixed torch version; CUDA wheel selection is handled by tooling
torch>=2.8.0                   # * PyTorch (use run/build scripts to pick CUDA wheels)